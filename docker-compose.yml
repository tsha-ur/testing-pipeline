services:
  ursim:
    image: universalrobots/ursim_polyscopex:${VERSION}
    privileged: true
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "80:80"
    network_mode: bridge